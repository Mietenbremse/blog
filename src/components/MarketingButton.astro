---
import { getLangFromUrl, useTranslations } from '../i18n/utils';
const {slug} = Astro.props;

const lang = getLangFromUrl(Astro.url);
---
<button class="bg-cipurple-1 rounded-full text-white px-4 py-2 self-start" type="button" onclick=`location.href='/blog/${slug}'`>
    Artikel Lesen
</button>